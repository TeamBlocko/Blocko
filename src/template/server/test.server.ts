/*
import { Workspace } from "@rbxts/services";
import BlocksSerializer from "template/shared/blocksSerializer";

const blocksSerializer = new BlocksSerializer();

const serialized = blocksSerializer.serializeBlocks(Workspace.Blocks.GetChildren() as BasePart[]);

print(serialized);

blocksSerializer.deserializeBlocks(serialized, Workspace);
*/

export {};
